// Ici j'écris le code qui me permettra de répondre à l'énoncé
// Écrire une fonction Javascript qui prend en paramètre 2 nombres et ensuite retourne leur somme.

function additionner(a, b) {
    return a + b;
  }
  
  // Exemple d'utilisation de la fonction :
  const nombre1 = 5;
  const nombre2 = 3;
  const somme = additionner(nombre1, nombre2);
  console.log("La somme est : " + somme);
  
